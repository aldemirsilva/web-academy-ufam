<div class="container">
  <p>Tecnologias baseadas no Node.js</p>
  <ul>
    {{#each technologies}}
      {{#if this.poweredByNodeJs}}
        {{{listTechnologies this}}}
      {{/if}}
    {{/each}}
  </ul>
</div>